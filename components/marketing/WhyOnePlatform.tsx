export function WhyOnePlatform() {
  return (
    <section className="relative py-24">
      <div className="mx-auto max-w-4xl px-6 text-center">
        <div className="mb-3 text-xs font-semibold uppercase tracking-widest text-[var(--ink3)]">
          Why one platform
        </div>
        <blockquote className="font-serif text-3xl leading-snug text-[var(--ink)] md:text-5xl">
          <span className="italic text-[var(--amber)]">&ldquo;</span>
          A Year Lead identifies an at-risk pupil in Arbor AI.
          Their class teacher uses ChalkAI to generate a differentiated worksheet for that same pupil.
          One login. One subscription. Work that actually joins up.
          <span className="italic text-[var(--amber)]">&rdquo;</span>
        </blockquote>

        <div className="mt-10 flex items-center justify-center gap-3 text-sm text-[var(--ink2)]">
          <span className="h-px w-12 bg-[var(--border2)]" />
          <span className="font-mono text-xs uppercase tracking-widest">Built for schools, not tech stacks</span>
          <span className="h-px w-12 bg-[var(--border2)]" />
        </div>
      </div>
    </section>
  )
}
